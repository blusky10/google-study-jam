apiVersion: v1
kind: ReplicationController
metadata:
  name: mail-service-rc
spec:
  replicas: 2
  selector:
    app: mail-service
  template:
    metadata:
      labels:
        app: mail-service
    spec:
      containers:
      - name: mail-service
        image: blusky10/mail-service
        imagePullPolicy: Always
        ports:
        - containerPort: 8080